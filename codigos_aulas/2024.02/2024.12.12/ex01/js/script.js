// window.addEventListener("load", function () {
// window.addEventListener("DOMContentLoaded", function () {
    function showMsg() {
        alert("Hello, World!")
    }
    
    // showMsg(); // executa a funcao imediatamente
    // setTimeout(showMsg, 10000); // executas a funcao 1x apos 10s
    setInterval(showMsg, 3000); // executa a funcao infinitamente a cada 3s

// })

